{
  "config": {
    "step": {
      "user": {
        "title": "Add text-to-speech engine",
        "description": "Provide configuration data. Fish Audio and other OpenAI-compatible endpoints are supported.",
        "data": {
          "api_key": "API key (optional if endpoint does not require auth)",
          "speed": "Speech speed",
          "model": "Model",
          "voice": "Voice",
          "url": "OpenAI-compatible endpoint URL (default: fish.audio)"
        }
      }
    },
    "error": {
      "wrong_api_key": "Invalid API key. Please enter a valid API key.",
      "already_configured": "This voice and endpoint are already configured."
    },
    "abort": {
      "already_configured": "This voice and endpoint are already configured."
    }
  }
}
